import {
  ArrowRight,
  BrainCircuit,
  ChevronDown,
  Clock3,
  Database,
  ExternalLink,
  Globe2,
  Mail,
  Map,
  Menu,
  Network,
  Play,
  Search,
  Satellite,
  Sparkles,
  TramFront,
  TriangleAlert,
  UsersRound,
  Workflow,
  X,
} from "lucide-react";

export {
  ArrowRight,
  BrainCircuit,
  ChevronDown,
  Clock3,
  Database,
  ExternalLink,
  Globe2,
  Mail,
  Map,
  Menu,
  Network,
  Play,
  Search,
  Satellite,
  Sparkles,
  TramFront,
  TriangleAlert,
  UsersRound,
  Workflow,
  X,
};

export function AtlasMark({ className = "" }: { className?: string }) {
  return <span className={`atlas-mark ${className}`} aria-hidden="true" />;
}

export function MilanMark({ className = "" }: { className?: string }) {
  return <span className={`milan-mark ${className}`} aria-hidden="true" />;
}
